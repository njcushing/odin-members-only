extends layout

block content
  h3 Become an administrator
  if (currentUser)
    p To become an administrator, enter the secret passcode into the box below:
    form(action="" method="POST")
      input#password(name="password" type="password" required=true)
      button(type="submit") Submit
    if (errors)
      for error in errors
        p= error.msg
  else
    p You must be signed in to become an administrator.
    a(href="/sign-in")
      button Sign In
  hr
  a(href="/")
    button Return to Home